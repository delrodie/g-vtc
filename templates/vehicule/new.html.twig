{% extends 'base.html.twig' %}

{% block extra_title %}Nouveau Vehicule{% endblock %}

{% block body %}

    <div class="text-end mt-3" data-controller="header">
        <a href="{{ absolute_url(path('app_vehicule_index')) }}" class="text-success fw-semibold fst-italic"><i class="bi bi-list-ul"></i> Voir la
            liste...</a>
    </div>

    <h4 class="page-title">Enregistrement des vehicules</h4>

    {{ include('vehicule/_form.html.twig') }}
{% endblock %}
