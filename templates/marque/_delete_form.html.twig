<form method="post" action="{{ path('app_marque_delete', {'id': marque.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette marque?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
