{% extends 'base.html.twig' %}

{% block title %}Edit Chauffeur{% endblock %}

{% block body %}
    <div class="text-end mt-5">
        <a href="{{ absolute_url(path('app_chauffeur_index')) }}" class="btn btn-outline-success btn-sm">
            <i class="bi bi-arrow-90deg-left"></i> Retour à liste
        </a>
    </div>

    <h4 class="page-title">Modification</h4>
    {{ include('chauffeur/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}

{#{{ include('chauffeur/_delete_form.html.twig') }}#}
