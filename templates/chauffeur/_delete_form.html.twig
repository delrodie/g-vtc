<form method="post" action="{{ path('app_chauffeur_delete', {'id': chauffeur.id}) }}" onsubmit="return confirm('Voulez-vous vraiment spprimer ce chauffeur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chauffeur.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
